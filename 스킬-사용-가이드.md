# 코드 품질 자동 검증 스킬 — 사용 가이드

> 이 스킬 세트는 [코드팩토리](https://www.youtube.com/@codefactory-dev)에서 설계한 구조를 기반으로 합니다.

## 이 스킬은 무엇인가요?

바이브 코딩이나 AI 코딩을 하다 보면 코드가 빠르게 늘어나면서 프로젝트의 규칙이 무너지기 쉽습니다. "컴포넌트는 이렇게 만들어야 해", "API는 이 패턴을 따라야 해" 같은 약속들이 세션이 바뀌면 잊혀지죠.

이 스킬 세트는 **프로젝트의 규칙을 코드로 정의하고, AI가 자동으로 검사하게 만드는 시스템**입니다. 사람이 하나하나 체크하지 않아도, 명령어 하나로 프로젝트 전체가 규칙을 지키고 있는지 확인할 수 있습니다.

---

## 구성 요소

스킬은 **세 가지 역할**로 나뉩니다.

### 1. `manage-skills` — 검증 규칙을 만드는 스킬

"시험 문제를 출제하는 선생님" 역할입니다.

코드를 수정하면 이 스킬이 변경사항을 분석해서, "이 부분은 아직 검사 규칙이 없네요"라고 알려줍니다. 그리고 필요한 검증 스킬(`verify-*`)을 새로 만들거나, 기존 스킬이 변경된 코드를 제대로 커버하도록 업데이트해줍니다.

### 2. `verify-implementation` — 검증을 실행하는 스킬

"시험을 채점하는 채점관" 역할입니다.

이 스킬 하나만 실행하면, 등록된 모든 `verify-*` 스킬을 자동으로 돌려서 통합 리포트를 만들어줍니다. 이슈가 발견되면 파일 위치, 문제 설명, 수정 방법까지 정리해주고, 원하면 자동으로 고쳐주기도 합니다.

### 3. `verify-*` — 실제 검증 규칙 스킬들 (프로젝트별로 생성)

"시험 문제" 자체입니다.

이것들은 여러분의 프로젝트에 맞게 만들어지는 개별 검증 규칙입니다. 예를 들어 `verify-api`는 API 패턴을, `verify-ui`는 UI 컴포넌트 규칙을 검사합니다. 처음에는 없고, `manage-skills`를 실행하면 자동으로 생성됩니다.

---

## 빠른 시작

### 전제 조건

- [Claude Code](https://docs.anthropic.com/en/docs/claude-code)가 설치되어 있어야 합니다
- 프로젝트가 Git으로 관리되고 있어야 합니다

### Step 1: 스킬 파일 복사

아래 파일들을 여러분의 프로젝트에 그대로 복사하세요:

```
프로젝트 루트/
├── .claude/
│   └── skills/
│       ├── manage-skills/
│       │   └── SKILL.md
│       └── verify-implementation/
│           └── SKILL.md
└── CLAUDE.md
```

`CLAUDE.md`에는 아래 내용을 추가하세요:

```markdown
## Skills

커스텀 검증 및 유지보수 스킬은 `.claude/skills/`에 정의되어 있습니다.

| Skill | Purpose |
|-------|---------|
| `verify-implementation` | 프로젝트의 모든 verify 스킬을 순차 실행하여 통합 검증 보고서를 생성합니다 |
| `manage-skills` | 세션 변경사항을 분석하고, 검증 스킬을 생성/업데이트하며, CLAUDE.md를 관리합니다 |
```

### Step 2: 첫 번째 검증 스킬 만들기

Claude Code를 열고 다음 명령을 실행하세요:

```
/manage-skills
```

AI가 여러분의 코드를 분석하고, "이런 검증 규칙이 필요합니다"라고 제안합니다. 승인하면 `verify-*` 스킬이 자동으로 생성됩니다.

### Step 3: 검증 실행

검증 스킬이 생성되었으면 이제 검증을 돌릴 수 있습니다:

```
/verify-implementation
```

등록된 모든 검증 스킬이 실행되고, 결과가 리포트로 정리됩니다.

---

## 사용 시나리오

### 일상적인 개발 흐름

```
코드 작성 → /verify-implementation → 이슈 확인 → 수정 → PR 생성
```

기능을 구현한 뒤 `/verify-implementation`을 실행하면, PR 올리기 전에 프로젝트 규칙 위반을 미리 잡을 수 있습니다.

### 새로운 패턴을 도입했을 때

```
새 패턴 도입 → /manage-skills → 검증 스킬 생성/업데이트 → /verify-implementation
```

예를 들어 새로운 캐싱 패턴을 도입했다면, `/manage-skills`가 "이 패턴을 검사하는 규칙이 없네요"라고 감지하고 새 `verify-caching` 스킬을 만들어줍니다.

### 이슈가 발견됐을 때

검증 결과에서 이슈가 나오면 세 가지 선택지가 주어집니다:

1. **전체 수정** — AI가 모든 이슈를 자동으로 고침
2. **개별 수정** — 이슈를 하나씩 검토하면서 고침
3. **건너뛰기** — 지금은 수정하지 않음

수정 후에는 자동으로 재검증이 돌아가서, 제대로 고쳐졌는지 확인해줍니다.

---

## 내 프로젝트에 맞게 변형하기

이 스킬은 그대로 가져다 써도 되지만, **자기 프로젝트에 맞게 변형해서 쓸 때 진가가 발휘됩니다.**

### 핵심 설계 원칙: 컨텍스트 레벨

이 스킬의 설계 철학은 "AI가 꼭 필요한 정보만 읽게 하자"입니다. 정보를 레벨별로 나눠서 계층화합니다:

| 레벨 | 무엇 | 언제 읽히나 |
|------|------|------------|
| 레벨 1 | `CLAUDE.md` | 매 세션 시작할 때 항상 (최소한의 정보만) |
| 레벨 2 | `SKILL.md` | 해당 스킬을 실행할 때만 |
| 레벨 3+ | 참조 파일들 | 스킬 실행 중 정말 필요할 때만 |

이렇게 하면 토큰을 아끼면서도 필요할 때 깊은 검사가 가능합니다. 새로운 verify 스킬을 직접 만들 때도 이 원칙을 기억하세요.

### verify 스킬 직접 만들기

`manage-skills`가 자동으로 만들어주지만, 직접 만들 수도 있습니다. `.claude/skills/verify-<이름>/SKILL.md` 파일을 만들고, 아래 구조를 따르면 됩니다:

```markdown
---
name: verify-<이름>
description: <한 줄 설명>. <언제 사용하는지>.
---

# <스킬 제목>

## Purpose
(이 스킬이 검사하는 항목 2~5개)

## When to Run
(이 스킬을 실행해야 하는 시점 3~5개)

## Related Files
(검사 대상 파일 목록 — 실제 경로만, 플레이스홀더 불가)

## Workflow
(구체적인 검사 단계. 각 단계에 사용할 도구, 파일 경로, PASS/FAIL 기준 명시)

## Output Format
(결과를 표시할 마크다운 테이블 형식)

## Exceptions
(위반이 아닌 케이스 2~3개 — false positive 방지용)
```

### 기존 스킬 수정하기

`manage-skills`가 자동으로 업데이트해주지만, 직접 수정할 수도 있습니다. 주의할 점은:

- 작동하는 기존 검사는 지우지 마세요 (추가/수정만)
- Related Files에는 실제로 존재하는 파일 경로만 넣으세요
- Exceptions에 새로운 예외 케이스가 있으면 추가하세요

### 스킬을 추가/삭제한 뒤 동기화할 파일 3곳

검증 스킬을 직접 추가하거나 삭제했다면, 아래 세 파일을 함께 업데이트해야 합니다:

1. **`manage-skills/SKILL.md`** — "등록된 검증 스킬" 테이블
2. **`verify-implementation/SKILL.md`** — "실행 대상 스킬" 테이블
3. **`CLAUDE.md`** — Skills 테이블

`/manage-skills`를 통해 스킬을 만들면 이 세 곳이 자동으로 업데이트되므로, 가급적 `/manage-skills`를 사용하는 것을 권장합니다.

---

## 실제 사용 예시 — Leaf App (React Native)

아래는 실제 React Native 프로젝트(스마트 HVAC 제어 앱)에서 스킬을 적용한 결과입니다.

### 프로젝트 현황

이 프로젝트에서 `/manage-skills`를 실행하여 생성된 verify 스킬은 4개입니다:

| 스킬 | 역할 |
|------|------|
| `verify-imports` | import 경로 규칙 검사 (상대경로 금지, 정렬 순서) |
| `verify-feature-structure` | 피처 도메인별 디렉토리 구조 일관성 검사 |
| `verify-component-patterns` | 컴포넌트 파일 구조 및 스타일링 패턴 검사 |
| `verify-query-hooks` | React Query 훅 패턴 및 네이밍 규칙 검사 |

### `/verify-implementation` 실행 결과 예시

```
## Implementation Verification Report

### Summary

| Verification Skill       | Status     | Issue Count | Details                              |
|--------------------------|------------|-------------|--------------------------------------|
| verify-imports           | PASS       | 0           | 상대경로 위반 없음                     |
| verify-feature-structure | 3 issues   | 3           | Query hook 디렉토리 누락              |
| verify-component-patterns| 37+ issues | 37          | default export 누락, 스타일 패턴 위반  |
| verify-query-hooks       | 1 issue    | 1           | queryKey 네이밍 불일치                 |

**Total issues found: 41**
```

### 발견된 이슈 예시

**verify-feature-structure에서 발견된 이슈:**

```
| # | File                          | Problem                                    | Fix                             |
|---|-------------------------------|--------------------------------------------|---------------------------------|
| 1 | src/hooks/queries/app/        | API 존재하나 Query hook 디렉토리 없음        | 디렉토리 생성 후 Query hook 작성 |
| 2 | src/hooks/queries/schedule/   | API 존재하나 Query hook 디렉토리 없음        | 디렉토리 생성 후 Query hook 작성 |
```

> 참고: 이 이슈들은 `verify-feature-structure`의 Exceptions에 "API-only domains (app, operatingHours, schedule, etc.)"로 정의되어 있어, 실제로는 면제 처리되는 케이스입니다. 이처럼 Exceptions를 잘 설정하면 불필요한 경고를 줄일 수 있습니다.

**verify-component-patterns에서 발견된 이슈:**

```
| # | File                                    | Problem                        | Fix                                       |
|---|-----------------------------------------|--------------------------------|-------------------------------------------|
| 1 | src/components/common/Badge/index.tsx   | default export 없음             | export default 추가                       |
| 2 | src/components/myInfo/EditMyInfo/index.tsx | import { Container } from './styles' | import * as S from './styles' 로 변경 |
```

> 이 시점에서 AI가 "37개 이슈가 발견되었습니다. 어떻게 진행할까요?"라고 물어보고, **전체 수정 / 개별 수정 / 건너뛰기** 중 선택할 수 있습니다.

### 스킬이 자동 생성한 verify 규칙 예시

`/manage-skills`가 프로젝트를 분석하여 만든 `verify-imports` 스킬의 실제 검사 로직 일부입니다:

```markdown
### Step 1: Detect relative import violations

**Tool:** Grep

**Check:** Find import statements using `../` in files under `src/`.

    grep -rn "from '\.\.\/" src/ --include="*.ts" --include="*.tsx"

**PASS criteria:** Results only contain Config or versionInfo imports
**FAIL criteria:** Relative `../` imports found other than Config/versionInfo

**Violation example:**
    // ✗ BAD
    import { Button } from '../../components/common/Button';

    // ✓ GOOD
    import { Button } from 'components/common/Button';
```

이처럼 각 스킬은 **어떤 도구로, 어떤 파일을, 어떻게 검사하고, 통과/실패 기준이 무엇인지**를 구체적으로 정의합니다.

---

## 스크린샷/녹화 캡처 가이드

직접 스킬을 사용하는 과정을 캡처해서 공유 자료에 넣고 싶다면, 아래 순서대로 진행하면 됩니다.

### 캡처 포인트 1: 스킬 파일 구조 (전체 모습)

**언제:** 스킬 설치 직후
**무엇을:** VS Code 사이드바에서 `.claude/skills/` 폴더를 열어서 전체 스킬 목록이 보이는 상태
**포인트:** "이렇게 프로젝트에 스킬 파일이 들어갑니다"를 보여줌

### 캡처 포인트 2: `/manage-skills` 실행 과정

**언제:** Claude Code 터미널에서 `/manage-skills` 입력 직후
**무엇을:** 3단계로 나눠서 캡처

1. **변경사항 분석** — "이 세션에서 N개 파일 변경됨" 테이블이 나올 때
2. **제안 액션** — "새 스킬 CREATE / 기존 스킬 UPDATE" 제안이 나올 때
3. **생성 완료** — 새 verify 스킬이 만들어지고 요약 보고서가 나올 때

### 캡처 포인트 3: `/verify-implementation` 실행 과정

**언제:** Claude Code 터미널에서 `/verify-implementation` 입력 직후
**무엇을:** 4단계로 나눠서 캡처

1. **실행 시작** — "다음 검증 스킬을 순차 실행합니다" 테이블
2. **검증 진행** — 각 스킬별 "검증 완료 — 검사 항목: N, 통과: X, 이슈: Y" 출력
3. **통합 리포트** — 최종 요약 테이블 (모든 스킬 결과가 한눈에 보이는 부분)
4. **수정 옵션** — "전체 수정 / 개별 수정 / 건너뛰기" 선택지가 나오는 부분

### 캡처 포인트 4: 수정 후 재검증 (Before/After)

**언제:** "전체 수정"을 선택한 후
**무엇을:** 2단계로 나눠서 캡처

1. **수정 적용** — "[1/X] verify-xxx: file.ts 수정 완료" 진행 상황
2. **재검증 결과** — "수정 전: X개 이슈 → 수정 후: PASS" Before/After 비교 테이블

### 녹화 팁

- **화면 녹화 도구:** macOS 기본 화면 녹화(Cmd+Shift+5) 또는 OBS 추천
- **터미널 폰트 크기:** 발표용이라면 16pt 이상으로 키우기
- **GIF 변환:** 짧은 클립은 [gifcap.dev](https://gifcap.dev)에서 GIF로 변환하면 문서에 넣기 좋음
- **구간별로 끊어 녹화:** 한 번에 다 찍기보다 위의 캡처 포인트별로 나눠서 녹화하면 편집이 쉬움

---

## 다른 AI 도구에서도 사용 가능

이 스킬 구조는 Claude Code 전용이 아닙니다. 스킬은 본질적으로 마크다운 파일이기 때문에, Cursor나 다른 AI 코딩 도구에서도 동일한 구조를 적용할 수 있습니다. 각 도구의 프롬프트 시스템에 맞게 SKILL.md의 내용을 참조하도록 설정하면 됩니다.

---

## 자주 묻는 질문

**Q: 처음 설치하면 바로 `/verify-implementation`을 실행할 수 있나요?**
아니요. 처음에는 검증 스킬(`verify-*`)이 없는 상태이므로, 먼저 `/manage-skills`를 실행해서 프로젝트에 맞는 검증 스킬을 생성해야 합니다.

**Q: verify 스킬은 몇 개까지 만들 수 있나요?**
제한이 없습니다. 다만, 영상에서 보여준 프로젝트는 18개의 verify 스킬을 운용하고 있을 만큼 많이 만들어도 `/verify-implementation`이 알아서 관리해줍니다.

**Q: 검사가 너무 엄격하면 어떡하나요?**
각 verify 스킬의 Exceptions(예외사항) 섹션에 "이건 위반이 아닌 경우"를 추가하면 됩니다. 이렇게 하면 false positive를 줄일 수 있습니다.

**Q: lock 파일이나 설정 파일도 검사하나요?**
아니요. `package-lock.json`, `yarn.lock`, 빌드 출력물, 문서 파일, 테스트 픽스처 등은 자동으로 면제됩니다.

---

## 한줄 요약

> `/manage-skills`로 프로젝트 규칙을 검증 스킬로 만들고,
> `/verify-implementation`으로 코드가 규칙을 지키는지 검사한다.
> 이 두 가지만 기억하면 됩니다.
